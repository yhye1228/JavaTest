BankAccount 클래스는 은행계좌 클래스 이다.
메소드 void deposit(double amount)   //입금
메소드 void withdraw(double amount)  //출금
메소드 void getBalance() //현재잔고 
세 메소드를 만들어 다음을 구현하세요.
class BankAccount {
 private String accountHolder; //예금주
 private double balance; //잔액
}

Main() {
  BankAccount ba = new BankAccount();
  ba.deposit(5000);
  ba.withdraw(2000);
  ba.getBalance();
}
------------------------------------------
5000원 입금완료, 현재잔액 : 5000원
2000원 출금완료, 현재잔액: 3000원
현재잔액 : 3000원
=====================================================================================================

class BankAccount{
    private String accountHolder;   // 예금주
    private double balance;         // 잔액

    // 메소드
    public void deposit(double amount){ // 입금
        this.balance += amount;
        System.out.print((int) amount + "원 입금완료, ");
        System.out.println("현재잔액 : " + (int)balance + "원");
        //System.out.printf("%.0f원 입금완료, ", amount);
        //System.out.printf("현재잔액 : %.0f원\n", balance);
    }
    public void withdraw(double amount){ // 출금
        if(balance < amount){
            System.out.println("출금액이 현재잔액보다 많아서 프로그램을 종료합니다.");
            System.exit(-1);/*************************************************************/
        }
        this.balance -= amount;
        System.out.print((int)amount + "원 출금완료, ");
        System.out.println("현재잔액 : " + (int)balance + "원");
        //System.out.printf("%.0f원 출금완료, ", amount);
        //System.out.printf("현재잔액 : %.0f원\n", balance);
    }
    public void getBalance(){//getter 이용 가능
        System.out.println("현재잔액 : " + (int)balance + "원");
        //System.out.printf("현재잔액 : %.0f원", balance);
    }

}

public class Main {
    public static void main(String[] args) {
        BankAccount ba = new BankAccount();
        ba.deposit(5000);
        ba.withdraw(2000);
        ba.getBalance();
    }
}

